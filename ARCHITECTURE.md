# 🏗️ 아키텍처 문서

올리브영 음성 쇼핑 어시스턴트의 시스템 구조와 설계 원칙을 설명합니다.

참고: [Pipecat 예제](https://github.com/pipecat-ai/pipecat/blob/main/examples/foundational/38-smart-turn-fal.py)

## 📐 시스템 아키텍처

```
┌─────────────────┐
│   Web Browser   │
│   (User UI)     │
└────────┬────────┘
         │ WebSocket (ws://)
         ↓
┌─────────────────────────────────────────┐
│      FastAPI Server (port 8000)         │
│  ┌───────────────────────────────────┐  │
│  │    WebSocket Endpoint (/ws)       │  │
│  └────────────┬──────────────────────┘  │
└───────────────┼─────────────────────────┘
                │
                ↓
┌─────────────────────────────────────────┐
│         Pipecat Pipeline                │
├─────────────────────────────────────────┤
│  Audio Input (WebSocket)                │
│    ↓                                    │
│  VAD (Silero - Voice Activity Detection)│
│    ↓                                    │
│  STT (Cartesia - Speech-to-Text)        │
│    ↓                                    │
│  User Context Aggregator                │
│    ↓                                    │
│  LLM (GPT-4 - Language Model)           │
│    ↓                                    │
│  TTS (Cartesia - Text-to-Speech)        │
│    ↓                                    │
│  Audio Output (WebSocket)               │
│    ↓                                    │
│  Assistant Context Aggregator           │
└────────┬────────────────────────────────┘
         │
         ↓
┌─────────────────┐
│  Store Service  │
│  (Data Layer)   │
└─────────────────┘
```

## 🔧 주요 컴포넌트

### 1. FastAPI 서버 (`src/server.py`)

**역할**: HTTP/WebSocket 엔드포인트 제공

**주요 기능**:
- WebSocket 연결 관리
- 웹 UI 제공
- Health check 엔드포인트

**API 엔드포인트**:
```
GET  /              # 웹 UI (HTML)
WS   /ws            # WebSocket 음성 연결
GET  /api/health    # 헬스 체크
```

**WebSocket 프로토콜**:
- **클라이언트 → 서버**: 오디오 데이터 (audio/webm Blob)
- **서버 → 클라이언트**: 오디오 데이터 (TTS 출력)

### 2. Pipecat 봇 (`src/bot.py`)

**역할**: 실시간 음성 대화 처리

**파이프라인 구조**:
```python
Pipeline([
    transport.input(),           # WebSocket에서 오디오 수신
    stt,                         # Cartesia STT (음성→텍스트)
    context_aggregator.user(),   # 사용자 발화 집계
    llm,                         # GPT-4 처리
    tts,                         # Cartesia TTS (텍스트→음성)
    transport.output(),          # WebSocket으로 오디오 전송
    context_aggregator.assistant() # 어시스턴트 응답 집계
])
```

**주요 특징**:
- 실시간 스트리밍 처리
- 인터럽션 지원 (사용자가 말을 끊을 수 있음)
- VAD를 통한 음성 활동 감지
- LLMContext를 통한 대화 히스토리 관리

**이벤트 핸들러**:
- `on_client_connected`: 클라이언트 연결 시 초기 인사말
- `on_client_disconnected`: 연결 해제 시 정리

### 3. 매장 서비스 (`src/store_service.py`)

**역할**: 올리브영 매장 데이터 관리 및 검색

**주요 메서드**:
- `find_store_by_name()`: 이름으로 매장 검색
- `find_store_by_location()`: 위치로 매장 검색
- `find_nearest_store()`: 랜드마크 기준 검색
- `search_by_service()`: 서비스로 검색
- `format_store_info()`: 정보 포맷팅

**데이터 구조**:
```json
{
  "stores": [...],      # 매장 정보 배열
  "categories": {...},  # 제품 카테고리
  "brands": {...}       # 브랜드 정보
}
```

## 🔄 데이터 흐름

### 음성 대화 처리 흐름

```
1. 사용자 음성 입력 (브라우저)
   ↓
2. MediaRecorder → WebSocket → FastAPI
   ↓
3. FastAPIWebsocketTransport → Pipecat
   ↓
4. VAD (Silero) - 음성 활동 감지
   ↓
5. STT (Cartesia) - 음성 → 텍스트 변환
   ↓
6. User Context Aggregator - 전체 문장 조합
   ↓
7. LLM (GPT-4) - 자연어 이해 및 응답 생성
   │ └→ Store Service 호출 (필요 시)
   ↓
8. TTS (Cartesia) - 텍스트 → 음성 변환
   ↓
9. Assistant Context Aggregator - 응답 기록
   ↓
10. FastAPIWebsocketTransport → WebSocket
   ↓
11. 브라우저 → 스피커로 음성 출력
```

## 🧩 통합 서비스

### OpenAI GPT-4 (LLM)

```python
llm = OpenAILLMService(
    api_key=self.openai_api_key,
    model="gpt-4o-mini"  # 또는 "gpt-4o"
)
```

- **역할**: 자연어 이해 및 응답 생성
- **기능**:
  - 컨텍스트 유지
  - 대화 흐름 관리
  - 매장 정보 기반 응답

### Cartesia (STT/TTS)

#### STT (Speech-to-Text)
```python
stt = CartesiaSTTService(
    api_key=self.cartesia_api_key,
    language="ko"  # 한국어
)
```

- **역할**: 음성 → 텍스트 변환
- **특징**:
  - 실시간 스트리밍
  - 한국어 지원
  - 높은 정확도

#### TTS (Text-to-Speech)
```python
tts = CartesiaTTSService(
    api_key=self.cartesia_api_key,
    voice_id="a167e0f3-df7e-4d52-a9c3-f949145efdab"
)
```

- **역할**: 텍스트 → 음성 변환
- **특징**:
  - 자연스러운 음성
  - 다양한 음색 선택
  - 낮은 지연시간

### Silero VAD

```python
vad_analyzer = SileroVADAnalyzer(
    params=VADParams(stop_secs=0.2)
)
```

- **역할**: 음성 활동 감지
- **기능**:
  - 음성과 비음성 구분
  - 발화 시작/종료 감지
  - 노이즈 필터링
  - `stop_secs`: 침묵으로 간주할 시간 (초)

### WebSocket Transport

```python
transport = FastAPIWebsocketTransport(
    websocket=websocket,
    params=FastAPIWebsocketParams(
        audio_in_enabled=True,
        audio_out_enabled=True,
        vad_enabled=True,
        vad_analyzer=vad_analyzer
    )
)
```

- **역할**: 실시간 양방향 오디오 통신
- **특징**:
  - 낮은 지연시간
  - 브라우저 기반 (설치 불필요)
  - 양방향 스트리밍

## 💾 데이터 관리

### 매장 데이터 (`data/store_data.json`)

**구조**:
```json
{
  "stores": [
    {
      "store_id": "D176",
      "name": "올리브영 강남역점",
      "address": "서울특별시 강남구...",
      "phone": "02-123-4567",
      "operating_hours": {
        "weekday": "10:00 - 22:00",
        "weekend": "10:00 - 22:00"
      },
      "services": ["화장품", "뷰티 디바이스", ...],
      "features": ["주차 가능", "와이파이 제공", ...],
      "nearby_landmarks": ["강남역 11번 출구", ...],
      "popular_products": ["토리든 다이브인 세럼", ...]
    }
  ],
  "categories": {
    "skincare": ["토너", "에센스", ...],
    "makeup": ["베이스", "아이", ...]
  },
  "brands": {
    "korean": ["설화수", "후", ...],
    "international": ["랑콤", "에스티로더", ...]
  }
}
```

**확장 방법**:
1. JSON 파일 직접 수정
2. 데이터베이스 연동 (향후)
3. CMS 통합 (향후)

## 🔐 보안 고려사항

### API 키 관리
- 환경 변수로만 관리 (`.env`)
- `.gitignore`에 `.env` 추가
- 프로덕션: 비밀 관리 서비스 사용 (AWS Secrets Manager 등)

### 통신 보안
- WebSocket은 `ws://` (로컬) 또는 `wss://` (프로덕션)
- 프로덕션에서는 HTTPS/WSS 필수
- CORS 설정으로 허용 도메인 제한 가능

### 데이터 프라이버시
- 대화 내용은 메모리에만 저장
- 세션 종료 시 자동 삭제
- 로깅 최소화 가능

## 📊 성능 최적화

### 지연시간 최적화
- **목표**: 500ms 이하 응답 시간
- **방법**:
  - 스트리밍 STT/TTS 사용
  - LLM 응답 청킹
  - VAD 민감도 조정 (`stop_secs`)
  - WebSocket 사용 (HTTP보다 빠름)

### 리소스 관리
- 동시 연결 수 제한 가능
- 세션 타임아웃 설정
- 메모리 사용량 모니터링

### 확장성
- 멀티 프로세스 지원 (Gunicorn + Uvicorn)
- 로드 밸런싱 가능
- 수평 확장 가능

## 🧪 테스트 전략

### 단위 테스트
```python
# tests/test_store_service.py
- 매장 검색 기능
- 데이터 포맷팅
- 필터링 로직
```

### 통합 테스트
- Pipecat 파이프라인 테스트
- API 엔드포인트 테스트
- WebSocket 연동 테스트

### E2E 테스트
- 실제 음성 대화 시나리오
- 다양한 질문 패턴
- 에러 처리 확인

## 🔄 배포 아키텍처

### 개발 환경
```
localhost:8000
├── FastAPI Server
├── Pipecat Bot
└── Local JSON Data
```

### 프로덕션 환경 (권장)
```
Load Balancer (NGINX)
├── App Server 1 (Docker)
│   ├── FastAPI + Pipecat
│   └── Gunicorn + Uvicorn
├── App Server 2 (Docker)
│   ├── FastAPI + Pipecat
│   └── Gunicorn + Uvicorn
└── Database (PostgreSQL)
    └── Store Data
```

**배포 명령어 예시**:
```bash
# Docker 빌드
docker build -t oliveyoung-voice-assistant .

# Docker 실행
docker run -p 8000:8000 \
  -e OPENAI_API_KEY=xxx \
  -e CARTESIA_API_KEY=xxx \
  oliveyoung-voice-assistant
```

## 📈 모니터링 및 로깅

### 메트릭 수집
- 응답 시간
- 대화 세션 수
- 에러율
- API 사용량
- WebSocket 연결 수

### 로깅 레벨
- **DEBUG**: 상세한 파이프라인 정보
- **INFO**: 일반 작동 정보
- **WARNING**: 경고 메시지
- **ERROR**: 오류 정보

### 추천 도구
- **Prometheus**: 메트릭 수집
- **Grafana**: 대시보드
- **Sentry**: 에러 추적
- **ELK Stack**: 로그 분석

## 🆚 Daily.co vs WebSocket 비교

| 항목 | Daily.co | WebSocket |
|------|----------|-----------|
| **설정 복잡도** | 높음 (API 키, 룸 생성) | 낮음 (서버만 실행) |
| **외부 의존성** | 있음 (Daily.co 서비스) | 없음 (자체 서버) |
| **비용** | 사용량 기반 과금 | 무료 (서버 비용만) |
| **확장성** | 매우 높음 | 중간 |
| **멀티룸 지원** | 지원 | 추가 구현 필요 |
| **WebRTC 기능** | 완전 지원 | 기본 오디오만 |
| **사용 사례** | 프로덕션, 대규모 | 개발, 단일 세션 |
| **지연시간** | 낮음 (WebRTC) | 매우 낮음 |

## 🚀 향후 개선 사항

### 단기 (1-3개월)
- [ ] 데이터베이스 연동 (PostgreSQL)
- [ ] 사용자 세션 관리
- [ ] 대화 히스토리 저장
- [ ] 다국어 지원 (영어, 일본어)
- [ ] 음성 품질 개선

### 중기 (3-6개월)
- [ ] 실시간 재고 정보 연동
- [ ] 제품 이미지 표시
- [ ] 예약/문의 기능
- [ ] 관리자 대시보드
- [ ] 모바일 최적화

### 장기 (6개월+)
- [ ] 개인화 추천 시스템
- [ ] 감정 분석
- [ ] 멀티모달 인터페이스 (음성+텍스트+이미지)
- [ ] 네이티브 모바일 앱
- [ ] 오프라인 모드

## 📚 참고 자료

- [Pipecat Documentation](https://docs.pipecat.ai/)
- [Pipecat GitHub](https://github.com/pipecat-ai/pipecat)
- [Pipecat Example: Smart Turn](https://github.com/pipecat-ai/pipecat/blob/main/examples/foundational/38-smart-turn-fal.py)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [WebSocket Protocol](https://developer.mozilla.org/en-US/docs/Web/API/WebSockets_API)
- [Cartesia Documentation](https://docs.cartesia.ai/)
- [OpenAI API Documentation](https://platform.openai.com/docs)

---

**질문이나 제안이 있으신가요?** GitHub Issues에 올려주세요!
